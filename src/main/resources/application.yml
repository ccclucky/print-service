server:
  port: 13572


# minio ????????
minio:
  endpoint: http://${REDACTED-ENDPOINT}
  accessKey: {REDACTED-ACCESSKEY}
  secretKey: ${REDACTED-SECRETKEY}
  bucketName: lab-print

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
